// Schema generation stub for @better-auth/cli.
import { createAuth } from "../auth";

// eslint-disable-next-line @typescript-eslint/no-explicit-any -- CLI stub requires any
export const auth = createAuth({} as any);
